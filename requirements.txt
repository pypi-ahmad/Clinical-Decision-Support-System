# Pinned production dependencies. Regenerate with:
#   uv pip compile pyproject.toml -o requirements.txt   (recommended)
# or:
#   pip-compile --output-file=requirements.txt pyproject.toml
#
# Torch wheels are resolved from the CUDA 13 index first. Use --index-url
# (exclusive) for torch if you must avoid PyPI resolution collisions.
--extra-index-url https://download.pytorch.org/whl/cu130

# Core runtime ---------------------------------------------------------------
fastapi>=0.115,<0.117
uvicorn[standard]>=0.32,<0.35
starlette>=0.40,<0.42
python-multipart>=0.0.18           # CVE-2024-53981 fixed in 0.0.18
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0

# Async I/O ------------------------------------------------------------------
aiofiles>=24.1.0
httpx>=0.27.2,<0.29

# Reliability & observability ------------------------------------------------
tenacity>=9.0.0
structlog>=24.4.0
slowapi>=0.1.9

# Observability (optional — instrumentation loaded lazily) -------------------
opentelemetry-api>=1.28.0
opentelemetry-sdk>=1.28.0
opentelemetry-instrumentation-fastapi>=0.49b0
prometheus-client>=0.21.0

# LLM providers --------------------------------------------------------------
ollama>=0.4.4
openai>=1.55,<2.0
anthropic>=0.40,<1.0
google-genai>=0.8.0                # NEW unified SDK (replaces google-generativeai)

# Agents & retrieval ---------------------------------------------------------
langgraph>=0.2.60
qdrant-client>=1.12.1

# OCR / document handling ----------------------------------------------------
pdf2image>=1.17.0
Pillow>=11.0.1                     # recent CVE fixes; pin minor bounds in prod
python-magic>=0.4.27; sys_platform != "win32"
python-magic-bin>=0.4.14; sys_platform == "win32"

# ML runtime (used by PaddleOCR-VL when enabled) -----------------------------
# Pin torch + torchvision as a matched pair; mismatched minor versions break
# the C++ ABI at import time. Override via a constraints file for CUDA vs CPU.
torch>=2.4,<2.6
torchvision>=0.19,<0.21

# UI -------------------------------------------------------------------------
streamlit>=1.41.0
streamlit-pdf-viewer>=0.0.19
pandas>=2.2.3

# HTTP (retained for any sync consumers; prefer httpx in new code) -----------
requests>=2.32.3                   # pulls Proxy-Authorization leak fix

# Dev / test -----------------------------------------------------------------
pytest>=8.3.3
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
